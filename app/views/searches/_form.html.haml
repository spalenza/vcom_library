= simple_form_for @search, html: { multipart: true }, remote: true do |f|
  = f.input :name
  = f.label :user_id
  = f.text_field :user_id
  = f.input :comments
  %br
  = f.button :submit, "Buscar"

#close
  %i.icon-chevron-up.icon-white
%br
%br